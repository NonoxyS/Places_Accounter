<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PlacesAccounter" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/background</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textAppearance">@style/TextAppearance.AppCompat</item>
        <item name="android:textColor">@color/deactive</item>
        <item name="android:editTextColor">@color/deactive</item>
        <item name="android:textColorHint">@color/deactive</item>
    </style>

    <style name="Theme.PlacesAccounter" parent="Base.Theme.PlacesAccounter" />
</resources>